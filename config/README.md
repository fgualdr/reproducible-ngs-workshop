# Configuration files

These files are examples and should be copied into student repositories before editing. The final teaching dataset may change, so accession values use placeholders unless validated.

